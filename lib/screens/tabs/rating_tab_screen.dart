import 'package:flutter/material.dart';


class RatingsTabScreen extends StatelessWidget {
  const RatingsTabScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Ratings"));
  }
}
